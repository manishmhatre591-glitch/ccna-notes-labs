# IPv4 Commands

```bash
show ip interface brief
show running-config
show ip route
ping 192.168.1.2
ipconfig
```

## Explanation

show ip interface brief → Displays interface status and IP addresses.

show running-config → Shows current configuration.

show ip route → Displays routing table.

ping → Tests connectivity.

ipconfig → Displays IP configuration on a PC.
```
